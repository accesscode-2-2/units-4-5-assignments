let testCases = Int(readLine(stripNewline: true)!)!

for var x = 0; x < testCases; x++ {
    
    //var first = Int(readLine(stripNewline: true)!)!
    
    print (readLine(stripNewline: true)!)
    
}